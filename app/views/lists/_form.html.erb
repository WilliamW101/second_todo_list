<%= form_for @list do |f| %>

<%= f.label :thing %>
<%= f.text_field :thing %>

<%= f.label :completed %>
<%= f.check_box :completed %>
<br />

<%= f.submit %>

<% end %>

<%= link_to 'Back', lists_path %>
